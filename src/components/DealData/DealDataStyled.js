import styled from "styled-components";

const DealDataStyled = styled.div`
   height:100%;
   width: 40%;
   display: flex;
	flex-direction: column;
   direction: rtl;
   padding-right: 5px;
   padding-left: 5px;

   .mainTitle {
      margin-top: auto;
   }
`;

export { DealDataStyled };
